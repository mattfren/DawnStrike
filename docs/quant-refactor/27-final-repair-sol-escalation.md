# Final Repair Sol Escalation Decision

Date: 2026-08-16
Decision: **LOCAL RECOVERY REQUIRED**

The first comprehensive repair used its two internal Luna correction cycles and
returned `REPAIR_REQUIRED`. This is the required Sol escalation because two
repair cycles failed; it is not an external blocker. The remaining failures are
test integration, gate mechanics, and Sol-owned closure work, all within local
repository authority.

## Accepted repair output

Retain, subject to the new final gate:

- the active-path SQLite guard and focused regressions;
- strict canonical return-truth classifier and 614-case adversarial matrix;
- mounted disabled opportunity producer, V5 adapter, catalyst evidence,
  telemetry, cache, current/historical adapters, and persistence/projection
  path;
- daily-publication compatibility repair;
- Bandit remediation;
- deterministic CI sharding;
- WP006 packet/supersession documentation and the proposed ledger matrix.

No global finding or requirement is closed by this decision.

## C-00 proof decision

The exact historical pytest node cannot be reconstructed after the fact because
the terminated `-q` run did not timestamp node execution and pytest temporary
roots were later pruned. Requiring an unknowable historical node name is not a
valid terminal gate.

C-00 may instead close only if all of the following are proven on the repaired
candidate:

1. the full 3,156-node inventory runs exactly once through deterministic
   shards;
2. the central guard is active before collection and blocks in-process,
   repository storage, URI read-only, direct-connect, and inherited subprocess
   access to `C:\r\dawnstrike-state\shadow_real.sqlite`;
3. no node opts into the operator-active-state marker during certification;
4. active-state SHA-256, length, mtime, schema, quick-check, and sidecars are
   identical before and after all shards;
5. the prior event remains documented as audit-pytest-attributable with the
   exact process/time/registry/Windows-event evidence and no installed-task run.

This substitutes current causal prevention and complete proof for unavailable
historical per-node telemetry. It does not erase the incident.

## C-01 repair decision

Production strictness is frozen. Do not restore `LEGACY_CONTRACT` eligibility,
expected-price fallback, unauthenticated fills, or inferred causal identity.

Repair stale tests and fixture builders to create exact canonical authenticated
entry receipts through `tests/_alpha_path_truth.py`. If a mounted production
bug is found, fix it only within the frozen canonical contract. Required green
collections include all label, outcome-capture, reconciliation, path replay,
trade watcher, dataset split, shadow, training, holdout, and model-integrity
tests plus the 614-case adversarial matrix.

## H-05 gate decision

The exact pre-commit secret hook requires the modified baseline to appear in
the Git index. Before Sol performs the real final stage, verification must use
a disposable `GIT_INDEX_FILE` initialized from HEAD and populated with the
complete candidate. This emulates final staging without modifying the real
index and must make the exact hook exit `0`. The baseline diff must contain only
reviewed false positives; candidate source remains scanned.

After Sol stages the final candidate, rerun the same exact hook against the real
index. No blanket allowlist or secret suppression is allowed.

## H-06 gate decision

Use `scripts/run_pytest_shard.py` to collect one canonical inventory and run all
16 shards. After source freeze, exactly two Luna gate owners may run disjoint
shard ranges in parallel (0-7 and 8-15). Their manifests must reconcile to the
canonical inventory with zero missing or duplicate node IDs and zero failure,
skip, xfail, or xpass outcomes. Any source change invalidates all shard results.

The active database must be captured before either owner starts and after both
finish. A hash or mtime change is immediate `HASH_DRIFT`.

## Remaining Sol-owned gates

H-01 and H-04 remain open until the source/test repair and shard evidence pass.
Sol will then adjudicate all 63 requirements and 14 prior findings, create the
final commit, verify from that commit, and issue certification. Luna must not
self-close ledgers, stage the real index, commit, push, or deploy.
